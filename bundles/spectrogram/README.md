# Spectrogram

Scrolling waterfall heatmap — each frame's 64-bin spectrum becomes a new
color column, scrolled in from the right. Migrated from the built-in
`spectrogram` style. No permissions, no network, no settings.

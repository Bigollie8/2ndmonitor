# Terrain

Spectrum history as a wireframe range receding into the distance.

## Notes

Pure 2D projection - no WebGL. Rows are scaled and offset by depth. Distinct
from freqgrid (flat) and tunnel (radial). Rows advance at a fixed ~10Hz.

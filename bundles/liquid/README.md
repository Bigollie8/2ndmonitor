# Liquid

Fluid metaball blobs (lava-lamp style) that swell and merge with bass, plus
mid-driven highlight ripples. Migrated from the built-in `liquid` style. No
permissions, no network, no settings.

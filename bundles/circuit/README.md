# circuit

A stable printed-circuit net whose pads glow with their frequency band; kicks inject hot pulses and hats inject fast ones, and the electricity walks the doglegged traces edge to edge. The board never moves - only the signals do.

No permissions, no network.

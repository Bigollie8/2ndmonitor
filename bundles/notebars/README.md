# Note bars

The chroma fold drawn as three octaves of piano keys - black keys dimmed,
every C highlighted. Musical cousin of Bars: pitch class instead of raw
frequency.

## Notes

Same chroma-fold approximation as the Chroma wheel. The three octaves share
one 12-value fold, so an octave envelope is applied to keep them from
rendering as three identical copies.

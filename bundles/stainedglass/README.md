# stainedglass

A leaded voronoi window whose panes glow with their frequency band; kicks send a light-through-the-window bloom drifting across the whole field. The window never changes - only the light does.

No permissions, no network.

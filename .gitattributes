# Generated by `npm run gen:sandbox` from app/src/sandbox/sandbox-html.ts and
# include_str!'d by app/src-tauri/src/sandbox.rs. Keep it byte-identical to
# what the generator writes (core.autocrlf would otherwise hand back CRLF on a
# Windows checkout, so the committed artifact and the regenerated one differ).
app/src-tauri/sandbox.html -text
